{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Tax Trail.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
